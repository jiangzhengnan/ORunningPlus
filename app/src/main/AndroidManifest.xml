<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.oplayer.orunningplus">
    <!-- 前台服务 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" /> <!-- 蓝牙搜索所需权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".OSportApplciation"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppLightTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".function.profile.MyProfileActivity"></activity>
        <activity android:name=".function.advanced.AdvancedSettingActivity" />
        <activity android:name=".function.test.Test2Activity">
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity android:name=".function.test.TestActivity" />

        <meta-data
            android:name="design_width_in_dp"
            android:value="360" /> <!-- 2340 1080  480  640 -->
        <meta-data
            android:name="design_height_in_dp"
            android:value="780" />

        <activity android:name=".function.connect.ConnectActivity" />
        <activity android:name=".function.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.BleService"
            android:exported="false" />
        <service
            android:name=".service.NotificationReceiverService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter android:priority="1000">
                <action android:name="android.service.notification.NotificationListenerService" />
                <!-- <action android:name="android.deletion.notification" /> -->
            </intent-filter>
        </service>
    </application>

</manifest>